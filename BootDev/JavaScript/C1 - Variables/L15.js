/*
TODO:
Assignment
Using the template literals and string formatting illustrated above, create a new variable msg and assign it the string:

Hi NAME, your open rate is OPENRATE percent

Replace NAME with the variable name.
Replace OPENRATE with the variable openRate.
*/


const name = "Saul Goodman";
const openRate = 30.5;

// ?
let msg = `Hi ${name}, your open rate is ${openRate} percent`
// don't touch below this line

console.log(msg);
